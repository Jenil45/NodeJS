const EVENTS = {
    ROOM_JOIN : "room_join",
    USER_JOINED : "user_joined",
    CALL_USER : "call_user",
    INCOMING_CALL_USER : "incoming_call_user",
    ACCEPTED_CALL : "accepted_call"
}

export default EVENTS